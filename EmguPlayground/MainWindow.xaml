<Window x:Class="EmguPlayground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmguPlayground" 
        xmlns:viewmodel="clr-namespace:EmguPlayground.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="339*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical">
            <Button x:Name="btnLoad" Click="BtnLoad_Click">Load ...</Button>
            <Button x:Name="btnRestore" Command="{Binding RestoreCommand}">Restore</Button>
            <Button x:Name="btnSave">Save ...</Button>
            <Button x:Name="btnGrayscale" Command="{Binding GrayscaleCommand}">Grayscale</Button>
        </StackPanel>
        <Image Name="imgPhoto" Stretch="Uniform" Grid.Column="1" Source="{Binding CurrentImage}"/>
    </Grid>
</Window>
